# 第一步实现进展 2026-04-21

本次已按“主表只保留标准元数据，指标全部下沉到明细表”的方向推进第一步后端改造。

已完成内容：

- 新增迁移脚本 [migrations/2026-04-21-refactor-quality-standards-step1.sql](/D:/Laibin/LaibinSugarInventory/migrations/2026-04-21-refactor-quality-standards-step1.sql)
  - `quality_standards` 增补标准编码、等级、版本、状态、备注、审计字段
  - 新增 `quality_standard_item`
  - 新增 `product_quality_standard_relation`
  - `assay` 新增采用标准、判定结果、失败指标、标准快照字段
  - 旧指标列从 `quality_standards` 中删除
- 后端新增正式的产品-标准关联接口
- 质量标准管理改为读取和写入 `quality_standard_item`
- 新增统一化验判定服务 `AssayStandardJudgeService`
- `AssayService` 与 `InStockService` 已接入新判定链路
- 新增化验详情查询接口 `GET /api/assay/{id}`

已完成的验证：

- 单测：`mvn -q -Dtest=AssayStandardJudgeServiceImplTest test`

当前边界：

- 这轮只收口数据库与后端，不包含第二步 Web / 小程序页面改造
- 第二步前端改造应基于新的详情结构字段：`judgeResult`、`judgeMessage`、`appliedStandard`、`matchedStandards`、`failedMetrics`、`standardSnapshot`
