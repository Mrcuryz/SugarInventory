# 智能仓储 AI 助手最终产品目标

适用项目：LaibinSugarInventory / 智能仓储
适用范围：Web AI 助手、Agent Gateway、MCP 工具编排、自然语言仓储操作体验
当前原则：本文描述最终产品体验和演进边界，不代表当前阶段已经全部实现。

## 1. 产品定位

智能仓储 AI 助手的最终目标不是展示 MCP 工具调用过程，而是提供接近用户日常使用网页 AI 对话的业务助手体验。

用户应当感觉自己在和“智能仓储助手”对话，而不是在和接口、工具、ID 或数据库字段对话。

最终体验应满足：

* 用户只需要说人话；
* 助手能理解上下文；
* 助手能主动追问；
* 助手能查询、分析、解释业务数据；
* 助手能用自然语言、候选按钮、业务卡片、追问建议和加载状态展示结果；
* 普通用户看不到 `toolName`、`productId`、`warehouseId`、raw JSON、`SUCCESS` 等内部字段；
* 管理员调试模式才显示工具调用、耗时、错误码和安全摘要；
* 查询和分析可以相对开放，但必须只读、受权限、可审计；
* 入库、出库、调拨等写操作必须遵循 `preview -> 用户确认 -> executionToken -> idempotencyKey -> execute`；
* 后端仍是最终权限判断者。

一句话：AI 助手应表现为业务助手，不是接口搜索器。

## 2. 分层边界

### 2.1 用户体验层

普通用户看到的是：

* 自然语言回答；
* 候选按钮；
* 结果卡片；
* 追问建议；
* 加载中的业务状态，例如“正在确认产品范围”“正在查询库存”“正在汇总库位分布”；
* 错误时的人话解释。

普通用户界面不得展示：

* `productId`；
* `warehouseId`；
* `toolName`；
* `SUCCESS`；
* raw JSON；
* error stack；
* Authorization Header；
* token、delegationToken、refresh token；
* 密码；
* 数据库连接信息；
* 内部服务器路径。

### 2.2 系统内部层

系统内部可以使用：

* MCP Tool；
* tool schema；
* agentSession；
* conversationContext；
* structured memory；
* tool result memory；
* delegationToken；
* audit log；
* errorCode；
* upstreamStatus。

这些内容用于安全执行、权限控制、调试、审计和问题排查，不直接暴露给普通用户。

### 2.3 调试模式

管理员调试模式可以查看：

* 工具名；
* 调用耗时；
* 结果状态；
* 错误码；
* 脱敏后的请求摘要；
* 脱敏后的响应摘要。

调试模式仍不得展示 token、Authorization Header、密码、完整异常堆栈或数据库连接串。

## 3. 产品原则

1. 用户和“助手”对话，不和接口、工具、ID 对话。
2. 普通用户界面默认隐藏 MCP 工具调用细节。
3. 助手必须支持连续上下文，例如“这些”“它”“刚才那个产品”“这个库位”。
4. 不确定时主动追问，不得猜产品 ID、库位 ID、托盘码或化验记录。
5. 查询/分析类能力可以使用受控只读数据访问层。
6. 不允许开放任意 SQL、任意 HTTP 代理、任意数据库访问。
7. 写操作必须强约束，不能让模型直接改业务数据。
8. 错误必须用业务语言解释，不得把权限错误、接口错误伪装成无数据。
9. token、Authorization、密码、内部异常、堆栈、数据库连接信息永不进入普通用户界面。
10. Agent Gateway 负责会话记忆、工具编排、权限上下文、流式事件和审计。
11. MCP Server 负责安全工具能力，不负责直接和用户对话。
12. 后端仍是最终权限判断者。

## 4. 体验目标

### 4.1 产品歧义消解

用户：

```text
帮我查黄冰糖当前库存
```

助手不应回答：

```text
resolve_products SUCCESS
```

助手应回答：

```text
“黄冰糖”有多个规格，你想查哪一个？

1. 黄冰糖（袋）
2. 黄冰糖（箱）净
3. 黄冰糖14.5（箱装）
4. 全部黄冰糖大类
```

这些选项应展示为业务候选卡片或按钮。普通用户不应看到内部 ID。

### 4.2 选择后继续查询

用户选择：

```text
黄冰糖（袋）
```

助手：

```text
黄冰糖（袋）当前库存为 11板30件，折合 470 件，总重量 11750kg。
```

### 4.3 上下文追问

用户：

```text
这些主要存放在哪些库位？
```

助手应沿用上一次唯一产品上下文，不要求用户重新输入产品名：

```text
黄冰糖（袋）主要存放在以下库位：

1. 2号库位：……
2. 3号库位：……

需要我继续展开具体托盘明细吗？
```

### 4.4 化验追问

用户：

```text
它今天有化验吗？
```

助手应沿用刚才的产品上下文查询化验。除非上下文已失效或仍存在歧义，不得重新要求用户选择产品。

### 4.5 库位自然语言

用户：

```text
2号库位现在还有多少容量？
```

助手应把“2号库位”归一化为业务库位名称后查询容量，并用业务语言回答，不暴露 `warehouseId`。

## 5. 业务对话记忆

Agent 必须具备持续上下文，而不是只把最后一句用户输入扔给模型。

至少应记住：

* 最近一次选择的产品；
* 最近一次查询的库存；
* 最近一次查询的库位；
* 最近一次查询的托盘；
* 最近一次查询的化验；
* 当前待用户选择的问题；
* 最近几轮聊天记录；
* 关键工具结果的安全摘要。

技术组成建议：

* message history；
* structured memory；
* tool result memory；
* pending clarification state；
* selected business entity state。

必须能接住类似表达：

* 这些在哪些库？
* 它今天化验了吗？
* 刚才那个产品最近30天怎么样？
* 这个库位还有多少容量？
* 给我展开详情。
* 导出一下。
* 换成白冰糖看看。

如果当前能力无法完成，例如导出或最近 30 天分析尚无安全工具，助手应说明能力缺口并给出可做的部分，不得假装完成。

## 6. 不确定性管理

真实助手不应乱猜，应主动追问。

示例：

* 用户说“查黄冰糖库存”，若存在多个产品，助手应询问“你想查全部黄冰糖大类，还是某个具体规格？”
* 用户说“查最近一天化验”，若“最近一天”不清楚，助手可以问“你是指今天，还是最近有化验记录的一天？”
* 用户说“把它入库”，若未来进入写操作能力，助手必须先列出缺少的产品、数量、库位、生产日期等信息，并进入 preview 流程，不得直接写入。

歧义路径要求：

* `UNIQUE`：可以继续查询；
* `AMBIGUOUS`：展示候选并询问用户，不得猜 ID；
* `NOT_FOUND`：说明未找到并给出建议，不得编造结果；
* 权限错误、接口错误、超时：用业务语言说明失败，不得解释为空数据。

## 7. 查询与分析能力边界

查询/分析类能力可以比当前 6 个工具更开放，但必须通过受控只读数据访问层实现。

允许方向：

* 读取当前用户有权限的数据；
* 读取白名单业务实体和字段；
* 返回结构化业务数据，而不是数据库裸字段；
* 对大数据做分页、聚合或采样；
* 生成业务摘要、趋势、异常解释和追问建议；
* 每次数据访问都审计。

禁止方向：

* 任意 SQL；
* 任意 HTTP 代理；
* 任意数据库访问；
* 不受限制的全量数据导出；
* 绕过后端权限；
* 把原始敏感字段直接塞给模型或普通用户界面。

## 8. 写操作安全边界

入库、出库、调拨、托盘确认、二维码作废/恢复、化验导入/更新/删除、质量标准修改等写操作必须强约束。

最终写操作链路：

```text
用户自然语言
  -> resolver / 信息补全
  -> preview
  -> 用户确认
  -> 后端签发 executionToken
  -> idempotencyKey
  -> execute
  -> 审计
  -> 业务结果回答
```

禁止：

* 模型直接改业务数据；
* 跳过 preview；
* 跳过用户确认；
* 跳过 executionToken；
* 跳过幂等键；
* 跳过权限校验；
* 让 Agent 临时拼接任意写接口请求。

## 9. 流式输出目标

流式输出应提升真实感，但展示的是业务进度，不是模型原始思考链，也不是单纯把最终回答拆成文本分片。

允许展示：

```text
正在确认产品范围……
找到多个黄冰糖规格，需要你选择。
```

或：

```text
正在查询库存……
正在汇总库位分布……
黄冰糖（袋）当前主要存放在……
```

禁止展示：

* chain-of-thought；
* 原始 MCP 协议消息；
* raw JSON；
* token；
* Authorization Header；
* 内部堆栈。

流式事件应至少区分三类：

* 用户可见业务事件：`message_start`、`progress`、`clarification`、`text_delta`、`card`、`error`、`message_end`；
* 管理员调试事件：`tool_start`、`tool_end`、`debug`；
* 系统控制事件：`heartbeat`、`cancelled`、`timeout`、`fallback`。

每个事件协议内部应具备 `messageId`、`eventId`、`sequence`、`type` 和 `payload`，用于前端去重、排序和审计定位。普通用户界面不直接展示这些协议字段。

当助手需要用户选择，例如“黄冰糖”存在多个规格时，流式输出应以 `message_end.finishReason=clarification_required` 结束，表示当前消息暂停等待用户输入，而不是普通完成。

流式中途失败时不得直接断开连接。已经发送 `message_start` 或 `progress` 后，如果工具调用超时或上游失败，应尽量继续发送业务 `error` 事件，再发送 `message_end`，并标记 `finishReason=timeout` 或 `error`。

前端渲染应以一个用户问题对应一个 assistant message 容器。`progress` 应作为容器内当前进度短句或可折叠处理过程，不应堆成多条独立聊天气泡。

## 10. 组件职责

### 10.1 Agent Gateway

Agent Gateway 负责：

* 绑定当前登录用户；
* 会话记忆；
* 工具编排；
* 权限上下文；
* 流式业务事件；
* 消歧状态；
* 错误解释；
* Agent Tool 审计；
* 面向前端返回自然语言、业务卡片和追问建议。

Agent Gateway 内部采用模块化 Agent 编排：主 Agent 负责意图、上下文、HITL、安全边界和最终回答，通过 Router 把受支持任务交给库存、库位、物流、二维码/托盘、生产、质量、分析报表、主数据、员工/RBAC 和审计最小权限专家。主 Agent 不直接持有业务工具；专家只看到本模块 context pack 和工具 schema。当前 10 个业务专家均已有 L1 只读或登记报表能力，但 v2/规划占位仍只能说明能力缺口，不得把设计条目当作已实现能力。

模块化 Agent 是 Python 侧的附加最小权限层，不能替代 Java Gateway 的用户身份、scope 和工具白名单校验。未来可以为不同专家配置不同模型与参数，但模型替换不得改变工具权限、参数校验、safe adapter 或写操作安全链路。详细设计见 `docs/agent/modular-agent-architecture.md`。

### 10.2 MCP Server

MCP Server 负责：

* 暴露安全、受控、可审计的工具能力；
* 调用后端只读或未来受控业务接口；
* 统一错误结构；
* 脱敏日志；
* 不直接和用户对话；
* 不保存用户密码；
* 不开放任意 SQL、任意 HTTP 代理或直接数据库访问。

### 10.3 后端业务系统

后端业务系统负责：

* 最终权限判断；
* token 与用户身份校验；
* agent_session 状态校验；
* 数据权限；
* 业务规则；
* 事务；
* 审计；
* 写操作 preview/execute 安全闭环。

## 11. 路线图

### M1.3.1：上下文指代修复

目标：

* 让助手支持“这些”“它”“刚才那个”“这个库位”“这条化验”等指代。
* 让候选卡片选择成为同一会话上下文，而不是新的用户消息。

允许做什么：

* 增强 Agent Gateway 会话记忆；
* 记录最近产品、库存、库位、托盘、化验和待选择项；
* 增加只读查询场景的上下文测试；
* 普通 UI 隐藏内部字段；
* 管理员调试模式查看安全摘要。

禁止做什么：

* 新增写工具；
* 新增任意 SQL 或任意 HTTP 代理；
* 暴露 token、Authorization Header 或内部 ID 给普通用户；
* 让模型猜产品 ID、库位 ID。

验收标准：

* “帮我查黄冰糖当前库存”展示业务候选卡片；
* 选择“黄冰糖（袋）”后回答库存；
* 追问“这些主要存放在哪些库位？”沿用产品上下文；
* 追问“它今天有没有化验？”沿用产品上下文；
* “2号库位现在还有多少容量？”能归一化并回答；
* 普通 UI 不显示 toolName、SUCCESS、productId、warehouseId；
* 会话撤销后上下文被清理。

### M1.3R-5：流式交互首版（已完成）

目标：

* 前端支持业务进度事件和最终回答流式展示；
* 用户在等待期间看到“正在确认产品”“正在查询库存”“正在分析结果”等业务状态。
* 流式协议支持 `messageId`、`eventId`、`sequence`、`finishReason`、`heartbeat`、`cancelled`、`timeout` 和 `fallback`。

允许做什么：

* 增加 Agent Gateway 流式事件协议；
* 增加前端流式渲染；
* 区分普通事件和管理员调试事件；
* 一个用户问题渲染为一个 assistant message 容器；
* 预留取消生成接口；
* 保持当前只读工具范围。

禁止做什么：

* 展示模型原始 chain-of-thought；
* 展示 MCP 原始协议帧；
* 为流式输出暴露 token 或 raw JSON；
* 新增写能力。

验收标准：

* 用户能看到业务进度；
* 最终回答可逐段或逐字输出；
* clarification 以 `finishReason=clarification_required` 暂停等待用户；
* 流中错误以 `error + message_end` 结束；
* 前端按事件 ID 去重并按 sequence 渲染；
* 普通模式不显示工具细节；
* 管理员调试模式可查看安全摘要；
* 权限错误和上游错误仍用业务语言解释。

### M1.3R-5.1：流式链路加固

目标：补齐流式审计、Java 端到端 SSE 测试、后端取消接口、客户端断开处理、真实浏览器验收和结果分类。

验收标准：

* 用户主动取消与客户端意外断开分别记录；
* 取消后不再向原 assistant message 写入工具结果或最终回答；
* 审计至少区分 `COMPLETED`、`CLIENT_DISCONNECTED`、`CLIENT_CANCELLED`、`PYTHON_TIMEOUT`、`PYTHON_ERROR`、`TOOL_TIMEOUT`、`TOOL_ERROR`、`SECURITY_FILTERED`、`FALLBACK_BLOCKED`；
* 长回答、候选等待、取消恢复、移动端卡片、连续消息和刷新状态通过真实浏览器验收。

### M1.3R-5.2：模型原生增量输出

目标：输出 LLM token delta，同时保留业务 `progress`、`clarification` 和 `card` 事件。

禁止展示 chain-of-thought、内部推理、raw MCP frame 或敏感工具数据。

### M1.3R-6：Human-in-the-loop 统一模型

目标：统一 clarification resume、preview 确认、execute 前确认，以及用户拒绝、修改和重新预览。

当前阶段只建设统一状态与协议模型，不开放入库、出库、调拨等执行能力。

阶段拆分：

* M1.3R-6a：协议与状态机，定义 `HitlInterrupt`、`ResumeAction`、`resumeToken`、状态机、`finishReason` 和审计码；
* M1.3R-6b：clarification 迁移，候选选择改成统一 interrupt/resume，前端只提交 opaque `optionId`；
* M1.3R-6c：Java resume API 与审计，Java 至少记录 interrupt 最小元数据并处理重复提交；
* M1.3R-6d：前端统一卡片，preview / execute confirmation 只占位，不触发写操作；
* M1.3R-6e：验收与文档，覆盖正常选择、重复点击、过期点击、刷新状态、不暴露内部字段和不触发写操作。

`resumeToken` 是前端可见的一次性能力凭证，必须短期有效、单次使用、绑定 `agentSessionId`、`interruptId`、`userId`、action 和 `optionId` / `previewId`，不可预测，不写普通日志，过期不可恢复。

候选项前端只展示 `optionId`、`displayLabel`、`description` 和 `supported`，不得暴露 `productId`、`warehouseId`、`inventoryId`。真实业务实体映射保存在后端 pending interrupt state。

产生人类介入时，当前消息必须以 `message_end.finishReason=interrupt_required` 暂停，而不是普通 `completed`。

### M1.4：受控只读数据分析层

目标：

* 让助手能查询和分析更多只读业务数据，例如库存分布、化验时间范围、化验报告详情、趋势分析。

允许做什么：

* 新增受控只读聚合接口；
* 新增白名单实体和字段；
* 支持分页、聚合、采样和摘要；
* 记录每次数据访问审计；
* 返回结构化业务数据和自然语言解释。

禁止做什么：

* 任意 SQL；
* 任意 HTTP 代理；
* 直接数据库访问；
* 无上限全量返回；
* 绕过当前用户权限；
* 把数据库裸字段直接暴露给普通用户。

验收标准：

* 可回答“最近30天黄冰糖化验趋势怎么样”等只读分析问题；
* 大数据查询有分页、聚合或采样；
* 每次数据访问可审计；
* 工具失败不被解释为空数据。

### M1.5：报表与导出

目标：

* 支持安全导出库存、化验或分析报表。

允许做什么：

* 基于已审计的查询或分析结果生成报表；
* 支持文件过期、下载权限和导出审计；
* 支持 Excel/PDF 等格式；
* 普通用户只看到业务文件名、摘要和下载入口。

禁止做什么：

* 无权限导出；
* 导出超出当前用户权限范围的数据；
* 导出 token、内部异常或数据库连接信息；
* 直接把任意 SQL 查询结果导出。

验收标准：

* 报表来源可追溯；
* 导出人、范围、时间、文件类型可审计；
* 下载链接有权限和过期控制；
* 失败时返回业务错误解释。

### M2：预览类业务操作

目标：

* 支持入库、出库、调拨等写操作前的 dry-run 预览，不修改业务数据。

允许做什么：

* 新增 preview 工具或后端聚合预览接口；
* 校验产品、库位、容量、化验、库存不足、冲突；
* 返回 blockingIssues、warnings、candidateResolutions 和 canExecute；
* 引导用户补齐缺失信息。

禁止做什么：

* 写库存；
* 创建真实业务单；
* 改变托盘状态；
* 分配真实二维码；
* 跳过用户确认。

验收标准：

* 预览不会修改任何业务表；
* 风险、缺失项和阻塞原因清晰；
* 用户确认前不能执行；
* 预览结果可审计。

### M3：执行安全层

目标：

* 为未来执行类工具建立安全基础。

允许做什么：

* executionToken；
* idempotencyKey；
* preview 与 execute 内容绑定；
* 强权限；
* 强确认；
* 操作审计；
* 执行前重新校验。

禁止做什么：

* 未经 preview 签发执行 token；
* 未经用户确认执行；
* 无幂等键执行；
* 让模型直接拼接写接口；
* 执行内容和预览内容不一致。

验收标准：

* 撤销、过期、重复请求、内容篡改都被拒绝；
* 审计可关联用户、会话、预览、执行和结果；
* 执行前重新校验业务状态。

### M4：执行类工具

目标：

* 在 M3 安全层基础上开放有限的入库、出库、调拨执行能力。

允许做什么：

* 首个候选 `execute_finish_inbound_task`；其他入库类型必须另行评审，不能复用通用入库执行占位；
* `execute_outbound_plan`；
* `execute_transfer_plan`；
* 返回业务单号、任务号和执行摘要。

禁止做什么：

* 开放通用写接口；
* 开放任意 SQL；
* 开放任意 HTTP 代理；
* 跳过 preview、确认、executionToken 或 idempotencyKey；
* 修改权限、质量标准等高风险配置。

验收标准：

* 执行只能基于有效预览；
* 幂等生效；
* 并发冲突可处理；
* 审计完整；
* 普通用户看到业务结果，不看到内部调用细节。

### M5：生产化与多用户能力

目标：

* 支持生产多用户并发、稳定运行、观测、权限隔离和持续运营。

允许做什么：

* HTTP/Streamable HTTP MCP；
* 请求级用户委托身份注入；
* 多用户会话隔离；
* 流式事件标准化；
* 可观测指标；
* unmet intent 统计；
* 运维告警；
* 更细粒度 scope。

禁止做什么：

* 多用户共用全局 token；
* STDIO 全局进程承载生产多用户；
* 把开发 STATIC_TOKEN 当生产授权；
* 记录敏感信息到日志或审计。

验收标准：

* 多用户并发隔离；
* 用户撤销会话后后续调用失败；
* token 不进入前端、LLM、MCP Tool 输出或普通日志；
* 审计和监控可定位问题；
* 生产部署文档完整。

## 12. 当前阶段提醒

当前第一阶段保留 51 个 L1 只读工具；第二阶段已在其上新增受控登记报表工具，旧备料池查询已从现行能力中移除。工具数量本身不是最终用户体验，后续迭代优先级应继续围绕“形成真实助手感”和上线安全闭环：

1. 上下文稳定；
2. 消歧自然；
3. 普通 UI 隐藏内部细节；
4. 错误解释可信；
5. 业务结果可读；
6. 查询分析受控开放；
7. 写操作强约束。

### 12.1 面向用户的结果呈现约定

Agent 的业务事实应优先通过结构化卡片展示；自然语言负责概括结论、解释异常和引导下一步，不重复堆砌卡片全部字段。

文本排版采用分层约束：确定性结果格式化器负责列表和段落的基本换行，前端保留换行，模型提示词只要求“多个并列事实逐行展示、较长结论自然分段”。不得强制所有回答套用统一标题或固定模板，以免削弱模型处理不同问题的表达能力。

普通用户界面只展示业务语义，不展示 `reportRef`、内部化验 ID、工具名等实现细节。仅当报告确实没有适用标准时，才使用“暂时无法自动判定，不代表产品不合格”等用户可读说明；已有适用标准且已完成判定的报告不显示该提示。

### 12.2 核心查询旅程的推广模板

库存与化验验证通过的交互模式，按真实用户旅程逐批推广，不按 MCP 工具数量机械复制。每条旅程至少同时具备：受控目标契约、可复用实体/筛选上下文、展示安全的事实信封、确定性完成判定、摘要加可展开卡片、自然语言追问和无写入副作用的回归测试。

首批推广顺序为：当前待处理任务、生产订单产出与去向、托盘生命周期。当前待处理任务只允许查询和筛选；“第一条任务详情”优先复用上一轮已经受控的任务事实，不再次读取或暴露内部任务 ID。任务查询不得被解释为确认、取消、入库、出库或调拨执行。

待处理任务卡片按可处理的业务类型分组，同组支持展开/收起、逐条选择和全选。用户点击“批量处理”时，前端只把所选托盘码交给现有任务处理弹窗；弹窗必须重新查询当前任务并校验任务仍为待处理且类型一致，再展示原业务表单。最终提交继续由当前登录用户在业务弹窗内完成，沿用原接口校验和审计；Agent/MCP 不生成写入参数、不调用写工具，也不把不同处理类型合并提交。

截至 2026-08-05，首轮 `L2_UI_HANDOFF_GO` 已在成品入库、成品出库和调拨上升级为三个独立 GoalContract、一个版本化 L2 预览工具。首次“处理当前任务”仍由主 Agent 委派 `logistics_expert` 调用 `query_pallet_tasks` 并强制 `PENDING`；用户选择相应托盘后，前端发起一条可见预览请求，再次经过“主模型理解 → 物流专家决策 → `preview_task_transition` → 结果分析”。工具只接受固定第 1 版、三个显式转换类型和 1～20 个托盘码。Java 同时校验 `task:view`、`task:confirm` 并重新读取当前任务；成品出库还必须核对托盘仍在库、产品仍为成品、绑定完整且当前库存存在；调拨必须按现有仓库左右侧、层数和产品可堆叠规则模拟整批目标位置。全部任务仍可处理时才允许打开类型严格匹配的既有业务弹窗；存在状态冲突时整批阻断。预览控制字段不进入普通回答或卡片，且没有任何 execute 工具消费它们；因此当前仍是正式 L2 预览，不是 L3 Agent 执行。

同日补齐了 L2 预览的服务端可恢复基础：只有 `READY` 结果会写入技术表，保存不可变响应快照、当前操作者、受信 Agent 会话、`task:view + task:confirm` 最小权限快照、所选托盘、状态摘要、规范化请求哈希、内容哈希和 5 分钟有效期。等价预览每次获得新的不透明引用，服务重启后仍可按用户和会话回读并验真；跨用户、跨会话、权限不足、过期或内容被修改均失败关闭。该记录不包含执行令牌或幂等键，当前没有确认、撤销、消费或 execute 入口，也不会改变任务和库存，因此只把 L3“确认绑定/持久化 HITL”门禁推进到 `PARTIAL`。

同日对首个 L3 候选进行了专项代码审计。候选被严格收窄为 `FINISH_IN + PENDING` 成品入库确认，不能因现有人工接口同时支持其他任务类型而扩大范围。现有 L2 预览仅证明任务资格，不能绑定最终仓库、日期、侧、数量、单位和备注；既有确认/取消锁策略、批量上限与顺序、独立 Agent 执行权限、幂等生命周期和原子执行审计也尚未闭合。因此当前只冻结 `preview_finish_inbound_execution → 受控界面显式确认 → 服务端 token/idempotency → execute_finish_inbound_task` 的未来协议，两个新工具均未登记、未实现、未启用，L3 继续 `NO-GO`。详见 `finish-inbound-l3-readiness-design-2026-08-05.md`。

2026-08-06 完成该候选的 S0 代码级业务加固：现有人工批量入库最多 20 条，整批在写入前完成规范化、别名去重和解析，并按托盘主键固定顺序处理；确认与取消统一使用同一托盘行锁入口，标签关联变化时失败关闭。单元合同已通过，但真实 MySQL 并发胜者和整批回滚仍需隔离数据库复验，因此该进展不改变 L3 `NO-GO`，也没有增加任何 Agent execute 工具或权限。

业务弹窗提交成功后，当前会话中引用同一托盘码的任务卡片必须同步为“已确认”，清除对应选择状态并取消其可选能力，同时重算卡片内的待处理与已确认数量。取消弹窗或提交失败时不得改写卡片状态。

生产追溯按“煮糖批次 → 关联生产订单 → 实际领料 → 产出及实际入库去向”分步完成。煮糖批次和生产订单均使用受控实体解析与可复用会话上下文；用户说“查看关联订单”“这个订单的原料消耗”“产出去了哪里”时，应复用上一轮已经确认的批次或订单，并重新读取相应的实时事实。结果使用可展开卡片展示批次用量、订单进度、来源托盘、产出数量、二维码进度及实际入库库位。

用户按“今天、昨天、最近一个月、本月、上周”等北京时间业务日期查询煮糖批次时，应直接查询该日期范围内的全部产品；产品名称只是可选筛选条件，不得被错误提升为必填条件。返回一个或多个批次时使用结构化候选卡片，用户选择后继续查询该批次流转；查询无结果时明确说明筛选范围，不进入无工具可执行的等待状态。

一个煮糖批次关联多个生产订单时，用户明确说“这两个”“这些”“全部”应在安全数量范围内一次返回多张订单卡片；用户未明确范围时应提供结构化订单选择，不要求用户手工输入内部 ID。批次投入的“预留/引用”必须作为独立阶段展示，不能计入实际领料、实际消耗或产出；卡片和回答统一使用中文业务状态，不向用户暴露 `PREPRINTED`、`USED_UP`、`RESERVED` 等后端枚举值。

“入库去向”必须比“计划目标库位”更严格：只有二维码已经形成库存记录、具有入库时间，或状态明确为已入库时才能展示为实际去向。尚未执行的任务目标不得冒充产出去向。当前阶段只做 L1 查询、上下文复用和结果呈现，不计算损耗率、产出率或自动生成生产计划，也不触发生产、领料、入库等写操作。

托盘生命周期按“托盘现状 → 最近流转 → 完整历史流转”推进。首次查询必须由用户明确提供托盘码；后续“它最近怎么流转”“再看完整历史”等追问复用受控 `CURRENT_PALLET`，不得由模型猜测或改写托盘码。托盘现状与历史流转分别登记 `PALLET_CURRENT_STATUS`、`PALLET_FLOW_HISTORY` 目标契约，并以事实信封和完成判定约束最终答案。

托盘卡片统一使用用户可读状态、产品、当前位置、数量、生产日期、化验摘要和流转时间线；不得展示原始事件枚举、数据库 ID 或内部控制引用。托盘尚未形成当前库存是正常业务状态，应回答“尚未入库”或“当前无库存仓位”，不得上升为系统故障。没有流转记录属于可完成的权威空结果，不等同于工具失败；若限定时间范围内没有流转，而生命周期中存在更早的登记事件，可将生命周期时间线适配为 `PALLET_FLOW_HISTORY` 事实，说明限定范围无数据并补充最近一次已知流转。当前阶段仍为 L1 只读能力，不执行作废、恢复、任务确认、出入库或调拨。

用户明确要求“完整历史”时，专家必须选择完整流转记录工具，不能用托盘现状或生命周期摘要卡替代；若模型选错，Runtime 只做意图—工具一致性拒绝并要求专家重新决策一次，不绕过专家直接执行。完整历史查询使用受控全量日期边界，但普通回答和卡片只显示“完整历史”，不得暴露 `2000-01-01` 等内部查询下界。直接指定托盘码查询历史也必须登记 `CURRENT_PALLET`，保证后续追问和 GoalContract 完整。

托盘码错误必须区分两类：格式或校验位不合法时提示用户核对托盘码；格式合法但系统不存在时返回权威无数据。两类结果都不得显示后端错误码、异常堆栈或内部审计措辞，也不得让专家对同一无效参数循环调用工具。

2026-07-23 真实浏览器验收已覆盖：托盘现状、在库位置、最近流转、完整 14 条历史、限定范围无数据回退、格式非法、合法但不存在，以及完整历史工具选错后的专家重判。验收页面保持“模型理解 → 专家决策 → 工具调用 → 结果分析”的 6 个受控阶段，未展示模型原始思维链。

当前库存质量筛选已从“产品库存 + 产品最近化验”的跨工具拼接升级为共享批次事实模型。托盘库存按 `product_id + pallet_code.production_date`、非托盘库存回退 `product_id + inventory.entry_date`，并按该日期关联最新版本化验；不使用 `inventory.assay_id`。质量专家现可分别处理“当前不合格库存”“符合指定完整标准”“原始指标满足数值条件”，三者结果均形成可展开卡片，且可通过受控 `CURRENT_ASSAY_REPORT` 继续查看某条化验详情。无化验、无标准和明确不合格必须保持不同语义。

### 12.3 专家 Agent 统一执行范式

库存、化验、生产以及后续接入的业务专家，在 `AGENT_PLANNING_MODE=llm` 时必须统一遵守：

1. 主模型理解当前用户目标，并决定直接回答、追问、执行已登记配方或委派一个最小权限专家；
2. 被委派专家依据自己的业务说明、受控上下文和工具 schema，决定追问、调用工具或结束回答；
3. Runtime 校验专家身份、工具白名单、参数 schema、受控实体引用、调用次数和权限后，才允许调用只读工具；
4. 工具结果先转换为展示安全的事实观察与业务卡片，再由专家分析并形成最终反馈；
5. CompletionEvaluator 校验实体和必需事实是否齐全，禁止模型在事实不完整时自行宣布完成。

确定性 Router、关键词规则和状态机只允许用于显式的 deterministic 模式、受控降级、安全校验或候选卡片组装；在 LLM 模式下不得先于主模型截获普通业务请求，也不得替代专家做工具选择。为了用户可理解和审计，界面可以展示“理解需求、选择专家、调用业务工具、分析结果”等受控执行阶段，但不得展示模型原始思维链、Prompt、内部 ID、令牌或原始工具参数。

截至 2026-07-27，52 个 L1 只读工具已按 43 个用户业务目标进入上述统一范式。`allowedTools` 表达一个目标内可使用的最小工具集，`evidenceTools` 只包含能够生成完成事实的主查询工具；产品/库位/生产实体解析、目录发现和详情下钻不能单独证明目标完成。同一工具在不同目标中可以是主证据或支撑能力，Runtime 必须以当前 GoalContract 为准，不能因一次支撑调用切换目标。

“全量进入合同”仅表示代码层的目标类型、专家归属、FactEnvelope 和 CompletionEvaluator 已贯通。它不等于所有自然语言表达均已通过现场验收，也不等于模型只负责最后润色：模型负责目标理解、专家决策和基于安全事实的结果分析；Runtime 独立负责授权、参数、事实有效性和完成裁决。

2026-07-27 第一阶段在本地/UAT 范围封板：43 个目标各完成 3 次主模型稳定性重放（129/129），Python/Java/MCP 与前端构建通过，25 项页面补充验收及 ADMIN/QC/STAFF 独立角色关键旅程通过。该结论冻结当前只读基线，但不替代正式生产发布验收；端到端问答仍常见约 10～23 秒，下一阶段必须继续记录完整链路 P50/P95、模型轮次和工具次数。

截至 2026-08-05，第二阶段已有五类分析目标和一个跨域入口目标、七个版本化报表定义进入统一链路，并有成品入库、成品出库、调拨三个独立 L2 任务预览目标。当前运行时合计 54 个 GoalContract、53 个 Java 仓储无业务写入工具（52 个 L1 只读/登记报表工具和 1 个 L2 预览工具）、1 个 Python 进程内知识检索能力、10 个 Java-backed 业务专家、1 个进程内知识专家和 7 个报表定义；七个报表共用受控 `run_registered_report`，三个任务预览目标共用闭合的 `preview_task_transition`，没有按业务类型复制工具。今日运营概览只允许北京时间今天，组合今日稳定登记产出、今日化验、确认领用与稳定产出两条独立序列、当前库存、今日创建托盘任务和当前待处理任务；它不把当前快照伪装成今日变化，也不计算计划达成率、良率、收率、损耗率、SLA、原因或预测。本地真实 HTTP 已验证运行、归档回读、同快照 XLSX 及历史/筛选拒绝；Chrome 外部模型自然语言问答已验证 6 个受控阶段、五区块卡片及安全事实一致性。原六个报表的已验收结论和库存生产门禁保持不变。任务预览能力族的封板与 L3 准入结论见 `docs/agent/task-transition-preview-family-closure-and-l3-gate-2026-08-05.md`。

2026-08-01 再次按本地数据库、迁移、Controller/Service 和 Mapper 对报表事实进行校正：
`production_order_material` 当前写入链路只产生 `PICKED`。用户确认领用后，同一事务立即写入领料快照和出库/库存流水、删除库存并释放二维码，因此 `PICKED + picked_at` 是现行“已确认生产领用扣减”事实。建表注释中预留的 `CONSUMED/RETURNED` 没有现行业务入口；`semi_prepare_pool*` 与 `production_consumption_record` 属于已停用旧流程，不进入当前 Agent、MCP 或报表统计。数据库没有计划顺延、返工、报废、损耗或 SLA 配置字段，
这些概念不再作为要求产品经理补充的报表前置条件，而是当前系统明确不支持的能力。通用 XLSX 已是现阶段
正式导出，不等待品牌模板。新增指标必须先证明具体字段、真实写入动作、时间语义、可达状态和稳定关联键。

除今日运营概览外的六个趋势/日报定义接入统一跨期比较，继续复用同一个 `run_registered_report` 和各自 GoalContract。今日运营概览是当日入口快照，明确拒绝历史日期和跨期比较。其余定义的上一等长期间由后端确定性推导，自定义对比期必须完整、合法且不与本期重叠；两期复用同一报表定义、版本、指标口径和筛选条件。比较结果进入同一个 `ReportRun`、业务卡片和 XLSX，不新增第二个可见 reportRun，也不让模型自行计算、挑选基准或给正负变化贴“好/坏”标签。库存趋势比较还必须保持两期数据源模式一致，禁止把回放期与正式快照期直接比较。

最小 `ReportRun` 持久化、历史读取和 XLSX 同快照导出也已覆盖上述七个报表。读取和导出重新校验所有者、当前权限、30 天有效期和快照 SHA-256；导出只使用已保存快照并生成独立审计引用，不重新计算报表。本能力只写报表技术快照和导出审计，不修改库存、生产、化验等业务数据，也不代表 PDF/CSV、订阅、库存趋势生产发布、生产订单阶段效率、良率或预测已经完成。大型登记报表提交给模型分析前必须做确定性压缩，但完整事实必须继续保留在业务卡片、`ReportRun` 和导出文件中，不能用模型上下文限制裁剪用户可见事实。

2026-08-03 本人历史报表分页列表和跨会话重开已覆盖七类登记报表。列表只返回当前用户、当前仍有全部权限、未过期且内容哈希校验通过的快照；重开显示原始不可变 `ReportRun` 并复用现有卡片与导出，不重新运行报表、不调用模型，也不把历史结果伪装成实时数据。本能力属于报表生命周期和 Web 交付，不新增 MCP 工具或业务写操作。到期快照已增加北京时间每日批量物理清理和独立清理审计；清理只删除过期 `ReportRun` 载荷，导出审计在正式保留策略确定前继续保留。历史刷新、PDF/CSV 和订阅仍未完成。

2026-08-01 RAG-03B 已完成工程接入：Python Runtime 在 10 个业务专家之外增加独立
`knowledge_expert`，只持有进程内 L0 `search_approved_knowledge`；新增
`PROCESS_KNOWLEDGE_QUERY` 和 `ENTERPRISE_KNOWLEDGE_QUERY` 两个 GoalContract，当前合计
50 个 GoalContract。仓储 Java Gateway/MCP 当前为 52 个工具，知识检索不进入其白名单或
registry hash。静态资料与实时业务混合问题在没有登记配方时要求拆分；知识证据使用确定性安全
格式器和来源引用，`NO_DATA` 与 `UNAVAILABLE` 分开处理。该切片仅通过 Python 工程测试，正式
corpus 仍为 `VALIDATED`，未发布 `current.json`，也未完成 Java/Web 的 RAG-03C/04 验收。

2026-08-01 RAG-03C 已完成工程接入：Java Gateway 对知识回答的非流式卡片和 SSE card 使用同一
显式白名单，只展示业务内容与可读来源；Python 通过内部 `reviewTrace.knowledgeAudit` 提供受控
专家、Goal、状态、知识域、corpus version 和证据数量，Java 复用既有审计表记录安全元数据，
不保存问题原文、evidence、路径、内部 ID、分数或凭据。身份继续只信任 `LoginUser`、Spring
Security authority 和已校验会话，前端身份字段不能覆盖。知识检索仍不是 Java/MCP Tool，52 个
工具 registry 不变；正式 corpus 仍为 `VALIDATED` 且未发布 `current.json`，Web 展示和真实浏览器
验收进入 RAG-04。

## 13. Agent v1、v2 与后续规划能力的调整路线

本节是后续路线。第一阶段当前保留 51 个基础只读工具；第二阶段通过登记和测试增加 1 个报表运行工具及 1 个支持三种显式转换的 L2 任务预览工具，当前白名单为 53 个无业务写入工具，唯一受控复合配方仍不改变。已停用业务的工具不因历史封板而继续保留。

1. Agent v1 日常只读查询覆盖已完成并封板；inventory、warehouse、logistics、pallet、production、quality、master_data、administration、audit 专家均具有真实只读能力和固定数据测试。
2. 最小 `ReportRun` 持久化、同快照 XLSX 导出、本人历史列表和跨会话原快照重开已经完成；库存专项也已落地统一不可变事件、上线基线、版本化日终快照、每日守恒对账、质量问题和连续 7 天发布门禁。2026-08-01 又补齐独立 Capture/Verify 调度、数据库互斥、零点可信窗口、运行证据和失败退出码；错过窗口时只告警，不做历史回填。本地/UAT 可显式运行历史回放验证完整产品链路，生产门禁仍必须由目标环境积累真实连续窗口后通过。
3. v2-A 已完成今日运营概览及生产登记产出日报、库存水平趋势本地回放、化验判定趋势、单指标化验趋势、生产领料—登记产出趋势和已登记托盘任务处理耗时七个报表定义；除当日入口概览外的六个定义完成统一跨期比较。`inventory_level_trend_v1@1` 的回放数据必须标注模拟且不推进生产门禁；`pallet_task_cycle_time_v1@1` 只表达系统任务创建到确认的登记耗时。生产订单中间阶段、标签到入库、良率、损耗和长期产出去向继续保持阻断；其后再评估化验文件暂存预览以及入库/出库/调拨 dry-run。
4. 完成 executionToken、幂等、实体/规则版本重检、审批、事务、HITL 和完整审计后，才允许逐项进入 v2-B execute。
5. 工作群先开放群安全查询、登记报表、提醒和 Web 跳转确认，不在群聊内执行 L3/L4。
6. 历史数据、指标、规则和模型评测稳定后，再建设只生成 advisory scenarios 的 planning_expert、预测、优化和仿真。
7. 当前只预留 organizationScope、plantScope、warehouseAreaScope；多组织真正启用前必须完成 scope 隔离和跨 scope 测试。

2026-07-29 库存趋势专项审计后，统一不可变库存事件、上线基线和版本化日终快照已经落地。旧调拨使用共享业务动作的调出/调入两条腿，新托盘调拨使用含来源和目标的单事件；上线基线与当前库存逐维度一致。本能力只开始积累可信历史，不代表库存历史趋势已经开放；每日守恒对账、异常发布门禁和连续快照窗口仍是前置条件，仍禁止把 `inventory.entry_date` 分布或可清理的托盘流转记录包装成历史趋势。

2026-07-30 每日产品全局/仓库守恒对账、异常持久化和连续 7 天发布门禁已经落地。当前仅有 2026-07-29 日终快照，因缺少 2026-07-28 期初快照，该日被正确标记为数据不足，门禁为 `BLOCKED`、0/7；不得虚构期初或回填通过结果。首个可完整对账日期为 2026-07-30，需等待 2026-07-31 零点期末快照。本能力仍未登记库存趋势 GoalContract、报表或工具。

2026-08-01 为解决本地开发机关机导致无法连续跨零点验收的问题，登记 `INVENTORY_LEVEL_TREND_ANALYSIS` 与 `inventory_level_trend_v1@1` 的双模式实现。生产模式仍只允许范围完全落入可信日终窗口；本地/UAT 必须显式开启模拟，以当前库存为锚点反向回放已登记流水，并在回答、卡片、历史快照和 XLSX 中标注“本地历史回放模拟”。真实 HTTP 验收对 `2026-07-14` 至 `2026-07-20` 的黄冰糖（袋）得到 7 个日点、550→790 件、13,750→19,750 kg，历史重开与 XLSX 导出成功；该结果不改变真实门禁 0/7。

长期设计继续禁止任意 SQL、任意 HTTP、万能管理工具、模型自由 DAG、专家自由互聊、直接修改库存，以及任何绕过 preview/HITL 的写入。详细设计和机器可读占位见：

- `docs/mcp-analysis/agent-expert-tool-blueprint.md`
- `docs/agent/expert-tool-roadmap-registry.yaml`
- `docs/agent/cross-expert-recipe-roadmap-registry.yaml`
- `docs/agent/data-semantic-roadmap-registry.yaml`
- `docs/agent/business-rule-roadmap-registry.yaml`
- `docs/agent/high-value-analytics-report-plan.md`
