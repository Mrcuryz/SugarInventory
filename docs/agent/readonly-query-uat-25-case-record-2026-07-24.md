# Agent v1 只读查询 25 项真实页面验收记录

验收日期：2026-07-24  
验收页面：`http://127.0.0.1:5173/`  
验收账号：陈思聪  
业务时区：北京时间（Asia/Shanghai）  
验收范围：`readonly-role-uat-matrix.md` 中尚未完成真实页面验证的 25 项只读目标  

## 结论

- 24 项功能验收通过，其中包括正常有数据、权威空结果、安全消歧流程，以及修复后通过的 Q09 产品质量配置聚合查询。
- Q09 已关闭：新增受控聚合事实后，可以按已确认产品同时查询适用质量标准和所属批量化验组；关系来自产品 ID 的精确绑定，不由模型按名称推断。
- G03 权限边界通过：当前账号无查看 Agent 回答复核记录的权限，系统正确拒绝；仍需使用授权审计角色补做正向查询。
- 本轮没有调用 L3/L4 写工具，没有确认入库、出库、调拨或修改任何库存业务数据。
- 自动化回归：Python Agent 全量测试 294 项通过；Java 根项目与仓储 MCP 全量测试通过。

因此，本批结果仍不能表述为“25/25 正向功能全部完成”。按产品验收口径，当前为：

- 功能正向通过率：24/25（96%）。
- 若把 G03 的正确权限拒绝计为安全边界合格：25/25（100%）；但这不能替代授权审计角色的正向查询。
- “只读查询”总目标已关闭 Q09 的事实缺口，仍需使用授权角色完成 G03 正向验收。

## 状态说明

- **通过**：目标理解、专家归属、工具调用、事实结果和用户反馈均符合预期。
- **部分通过**：请求中的部分事实可以回答，但仍存在明确能力缺口。
- **权限边界通过**：当前角色被正确拒绝，没有越权，也没有把拒绝误报为无数据。
- 下表“耗时”为浏览器端从发送到最终消息出现的近似端到端时间，不是服务端纯工具耗时。

## 逐项输入、流程与实际输出

| 编号 | 用户输入 / 具体流程 | 实际输出摘要 | 约耗时 | 结论 |
| --- | --- | --- | ---: | --- |
| Q03 | 输入：`库存中有哪些不合格产品？` | 当前全部产品库存中未查询到明确不合格库存；回答没有把“无标准”误写成“不合格”。 | 未可靠计时 | 通过 |
| Q04 | 输入：`哪些当前库存符合黄冰糖 v1标准？` | 当前库存中没有查询到符合“黄冰糖 v1”完整标准的记录；标准名称被按标准身份处理，没有先误解析成产品。 | 10.9 秒 | 通过 |
| Q05 | 输入：`库存中哪些产品的蔗糖分不低于99.7？` | 当前库存中未查询到满足“蔗糖分 ≥ 99.7”的记录；按原始指标条件查询，没有错误转成质量标准查询。 | 5.5 秒 | 通过 |
| Q06 | 第 1 轮：`哪些当前库存缺少最近化验数据？`；助手追问“最近”天数。第 2 轮：`最近30天`。 | 最近 30 天缺少有效化验的当前库存只有“黄中冰（袋）40kg/件 25件/板”，共 4 板 0 件、约 4000kg，位于 1 号和 5 号库位。多轮追问保持原 GoalContract，没有再次要求产品范围。 | 约 7.7 + 8.6 秒 | 通过 |
| Q07 | 输入：`最近一个月有哪些化验异常？` | 查询到 1 条无适用标准的化验记录：黄冰糖（袋），日期 2026-07-14；回答将“无标准”和“不合格”分开表达。 | 12.6 秒 | 通过 |
| Q08 | 输入：`哪些当前库存没有适用的化验标准？` | 黄中冰（袋）40kg/件 25件/板共 4 板 0 件、约 4000kg，分布于 1 号和 5 号库位；明确说明“没有适用标准，无法自动判定，但不等同于不合格”。 | 24.1 秒 | 通过 |
| Q09 | 第 1 轮：`查询黄冰糖适用的化验标准和化验组`；按候选卡选择“黄冰糖（袋）25.0kg/件 40件/板”。 | 首轮正确识别产品歧义并展示具体产品候选；选择后候选卡收起为“用户已选择”摘要。聚合结果显示适用质量标准为 `GB` 版本 1、标准名称“黄冰糖”、已启用且为默认标准；所属批量化验组为当前未配置。数据库事实和工具/API 审计一致，未暴露产品 ID、标准 ID 或化验组 ID。 | 约 8.5 + 10.3 秒 | 通过 |
| L02 | 输入：`查询最近7天的入库单` | 最近 7 天未查询到入库单记录；按入库单类型和北京时间日期范围查询，没有伪造统一库存流水。 | 8.8 秒 | 通过 |
| L03 | 输入：`查询最近一次自动入库批次` | 最终输出“当前没有找到最近登记的自动入库批次记录”。空结果作为权威结果返回，没有再出现完成证据引用错误。 | 8.3 秒 | 通过 |
| P03 | 输入：`当前有哪些托盘异常？` | 当前未查询到托盘异常；空结果与工具失败分开表达。 | 17.4 秒 | 通过 |
| P04 | 输入：`哪些二维码已经打印但还没有入库？` | 查询到已打印 3 个、已入库 2 个、尚未入库 1 个；未入库项对应标签批次 `PO202606300001-LB001`、托盘码 `BT0010Z4`。 | 17.9 秒 | 通过 |
| P05 | 输入：`查询二维码批次20260724的入库完成情况` | 未查询到该批次的入库完成记录；按指定批次返回权威空结果，没有猜测或改写批次号。 | 32.1 秒 | 通过 |
| P06 | 输入：`查询黄冰糖的空闲固定二维码`；按候选卡选择具体产品。 | 所选黄冰糖产品当前没有空闲固定二维码；选择后继续原查询，没有重复要求选择产品。 | 9.2 秒 | 通过 |
| W04 | 输入：`查询黄冰糖（袋）最近30天库存台账` | 返回 2026-06-25 至 2026-07-24 的当前库存台账快照：2 条记录，均位于 21 号库位，托盘 `BT0015OY`、`BT001BT9`，合计 2 托；明确说明这是当前库存快照，不是历史流水。 | 23.9 秒 | 通过 |
| W05 | 输入：`查询当前备料池余额` | 返回 2 条正余额，均为黄中冰 4-15 目：2026-04-27 剩余 25 件/1000kg，2026-04-23 剩余 20 件/800kg；说明正余额不代表已为订单保留。 | 12.3 秒 | 通过 |
| W06 | 输入：`查询2号库位当前状态` | 2 号库位状态为“临期预警”，最大容量 120，当前托盘/占用 5，剩余 115，占用率 4.17%。 | 14.5 秒 | 通过 |
| W08 | 输入：`查询2号库位最近7天的操作`；系统先解析并确认 2 号库位，再查询北京时间范围。 | 返回 2026-07-18 至 2026-07-24 共 2 条已登记托盘入库操作，均发生于 2026-07-20 14:31:04，操作人陈思聪，产品黄冰糖（袋），托盘 `BT001BGW`、`BT001B3J`。 | 24.7 秒 | 通过 |
| W09 | 输入：`哪些库位存在混放？` | 返回 1 号、5 号库位存在多产品混放，均同时包含黄中冰和黄冰糖（袋），各涉及 2 个产品/筛网/状态组合。 | 18.6 秒 | 通过 |
| R06 | 输入：`查询生产订单 PO202606300001 的标签完成情况` | 订单已预打印；1 个标签批次、预留 1 张、已使用 0、已回收 0；批次 `PO202606300001-LB001`，产品黄中冰，状态显示为中文“已打印”，打印时间 2026-06-30 14:23:01。 | 12.8 秒 | 通过 |
| R07 | 输入：`当前有哪些在制物料？` | 返回 2 个黄中冰半成品，关联订单 `PO202605070003`：托板 `BT0010Z4` 位于 5 号库位、托板 `BT000YGI` 位于 1 号库位，各 25 件/1000kg。 | 12.1 秒 | 通过 |
| R08 | 输入：`生产订单 PO202606300002 当前可选择哪些领料候选？` | 返回 4 个黄中冰半成品候选：`BT0011CI`、`BT0011PV` 位于 1 号库位，`BT001229`、`BT0012FM` 位于 5 号库位；均为 2026-05-07、1 板/1000kg。 | 14.0 秒 | 通过 |
| M02 | 输入：`查询当前筛网目录` | 返回 8 个筛网名称：20MM以下、12-25、15-30、10、4-15、12以上、15mm以上、无筛网。 | 7.0 秒 | 通过 |
| A01 | 输入：`查询当前仓管员名单` | 返回仓库部门在职人员 4 名：杨让、韦海宾、韦扬猛、黄河清；手机号已脱敏，岗位显示为管理员或叉车。 | 31.9 秒 | 通过 |
| G01 | 输入：`查询今天的业务操作日志` | 输出“今天（2026-07-24）未查到任何业务操作日志”；北京时间日期解释正确。 | 6.4 秒 | 通过 |
| G03 | 输入：`查询待复核的 Agent 回答` | 输出“当前查询被权限边界拒绝”；没有泄露审计数据，也没有降级为“无数据”。 | 7.5 秒 | 权限边界通过 |

## 本轮发现并修复的问题

1. 多轮“最近 N 天”补充范围时，活动目标可能保留但全局产品/库位范围缺省，导致参数校验失败。现按活动 GoalContract 补齐受控 `ALL` 范围。
2. 模型已得到权威空结果但连续漏引证据时，Runtime 原先会返回完成校验错误。现只在 FactEnvelope 已完整、无工具错误且证据工具明确返回 `NO_DATA` 时使用安全空结果兜底。
3. 主模型识别到正确 GoalContract、但错误选择目标归属专家时，原先直接拒绝。现允许一次受控路由纠正，不放宽专家工具白名单。
4. “仓管员名单”和“角色权限目录”、“指定库位最近操作”和“全局业务审计日志”存在语义混淆。主模型提示已明确业务边界。
5. 仓库近期操作工具把北京时间 RFC3339（例如 `2026-07-18T00:00:00+08:00`）直接转发给只接受本地时间的 Java DTO，导致 400。仓储 MCP 现统一转换为北京时间本地日期时间后再调用后端。
6. 本地验收启动配置必须显式启用 9 个已登记只读专家；只启用默认 6 个专家会让基础资料、后台管理和审计目标被错误拒绝。

## 未关闭事项

### Q09：产品与化验组关系（已关闭）

- 新增 GoalContract `PRODUCT_QUALITY_CONFIGURATION` 和 L1 工具 `query_product_quality_configuration`，按已确认产品一次返回当前适用质量标准与所属批量化验组。
- 产品先经 `resolve_products` 消歧，Runtime 只把受控 `CURRENT_PRODUCT` 转换为后端 `productId`；模型不可直接提供或猜测内部 ID。
- 化验组关系读取现有 `assay_group.related_products`，使用产品 ID 精确匹配；短期不按产品名称推断。后续数据库演进仍建议把逗号分隔字段规范化为关系表。
- 聚合接口保留 `quality_standard:view` 与 `assay:view` 双权限校验。真实验收首次暴露“委托令牌只读路径白名单漏登记”，补齐后工具审计为 `SUCCESS`、上游 API 为 HTTP 200。
- 补充输入“系统当前有哪些化验组？”仍由原 `ASSAY_REFERENCE_DATA` 目录目标处理，成功返回 5 个化验组，没有被新产品聚合目标错误接管。
- 补充输入“系统当前有哪些化验组？”仍由原 `ASSAY_REFERENCE_DATA` 目录目标处理，成功返回 5 个化验组，没有被新产品聚合目标错误接管。

### G03：授权角色正向验收

当前账号的权限拒绝符合预期，但只证明了安全边界。需要使用具备 Agent 回答复核权限的审计角色再次执行同一输入，验证：

- 待复核状态过滤正确；
- 分页和数量一致；
- 回答不暴露内部引用、原始工具名或数据库字段。

2026-07-24 已新增最小只读权限迁移
`migrations/2026-07-24-add-agent-review-view-permission.sql`：只创建
`agent:review:view` 并默认授予 `ADMIN`，不授予 `agent:review:update`。
用户回答层已将回答状态、置信程度、问题环节和复核进度统一转换为中文，
且未知枚举不会回退展示原始内部值。迁移已在当前验收库执行，已确认
`ADMIN` 只有 1 条查看权限绑定、没有回答复核修改权限；当前验收账号“陈思聪”
属于 `ADMIN`。Python Agent 全量 295 项测试以及 Java 审计/复核定向测试已经通过。
服务启动后使用该账号完成浏览器正向查询，即可关闭本项。

## 后续建议

1. 将本文件作为本批 25 项的可追溯验收证据，不用截图代替事实记录。
2. 完成 G03 授权审计角色正向验收后，再按 `readonly-role-uat-matrix.md` 复核其余已标记“代表性通过”的目标是否需要不同真实角色补测。
3. 在宣布“只读查询产品目标完成”前，增加稳定性重放：每个目标至少重复 3 次，统计目标路由一致率、工具成功率、事实一致率和 P95 延迟。
4. 报表分析和 L3 写入仍不应提前进入本轮验收结论。
