# 仓库平面图散件板位高亮

## 变更

1. “查看库位明细”弹窗的单板详情新增“数量”字段。
2. 整板仍显示为“1板”。
3. 散件按库存占位规则显示为“x件，占1板位”。
4. 库位详细平面图右上角新增圆形“散”按钮，用于查看散件板位。
5. 点击后，当前库位内所有 `pieces > 0` 的板位会按选中状态高亮；再次点击取消高亮。

## 口径

散件仍占用一个板位，但实际数量按 `pieces` 展示。该高亮只用于查看，不改变筛选条件和库存数据。
