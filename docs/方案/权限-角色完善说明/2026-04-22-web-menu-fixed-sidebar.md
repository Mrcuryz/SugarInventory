# Web 侧边菜单固定

日期：2026-04-22

本次调整仅处理 Web 管理端整体布局滚动归属，不修改菜单结构与权限逻辑。

调整内容：
- 页面根容器改为固定视口高度 `100vh`
- 左侧菜单栏固定在视口内，并独立支持纵向滚动
- 顶部页面标题栏与标签页收口为固定顶部区域
- 右侧主内容区单独滚动，避免整页滚动时带动菜单一起移动

涉及文件：
- `webpage/src/components/Menu.vue`
