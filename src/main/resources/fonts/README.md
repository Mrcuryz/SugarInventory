# Fonts

`NotoSansSC-VF.ttf` is bundled so QR label product names render correctly in Linux/Docker environments without system Chinese fonts.

The renderer loads this font from the classpath before trying host system fonts.
