# 首页工作台修复说明

本次修复首页库位占用概览与库位操作入口：

- 首页库位占用概览改为复用库位台账页同一套 `/inventory/query` 数据源，避免旧概览接口与库位管理页占用率算法不一致。
- 首页占用率展示改为按后端返回百分比直接取整并限制在 `0-100`，与库位管理页保持一致，不再把 `<= 1` 的值当作小数比例放大。
- 首页“查看”库位不再展开旧版无二维码的库位详情与出入库操作区，改为跳转到 `/warehouse-map?warehouseId=...`，由仓库平面图自动选中对应库位并展示详情。
